type Movie {
  id: Int!
  title: String!
  year: Int!
  rating: Float!
  summary: String!
  medium_cover_image: String!
  genres: [String]!
}

type Query {
  movies(limit: Int, rating: Float): [Movie]!
  suggestions(id: Int!): [Movie]!
}
